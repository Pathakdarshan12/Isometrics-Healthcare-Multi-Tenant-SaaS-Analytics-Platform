version: 2

models:
  - name: fct_sla_monitoring
    description: "SLA compliance monitoring"
    columns:
      - name: hospital_id
        tests:
          - not_null

  - name: fct_cost_attribution
    description: "Cost tracking per hospital"
    columns:
      - name: hospital_id
        tests:
          - not_null

#  - name: fct_hipaa_audit_trail
#    description: "HIPAA audit logging"
#    columns:
#      - name: audit_id
#        tests:
#          - not_null
#          - unique